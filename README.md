# Comparison of FEROS calibs
+ both for flat field images and wavelength calibration

+ the script reads the exposure times to make sure to compare  apples with apples
+ if only one exposure time was available, at least we know how to scale it
+ I selected 3 lines, one from the blue, medium and red range
+ the red line (right column) is always the most affected when the lamp was failing or was replaced.
